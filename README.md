# youtubez


## Build and Run

```docker-compose up```

This will build and run the application:
1. Builds the docker image for the web app
2. Starts up a postgresql db instance in docker
3. Starts up the web app instance in docker
4. Accessible by http://localhost:8080


### TODO

1. Cleanup DB code (refactor with a framework)
1. Refactor upload success/failure page to redirect to Home
1. Frontend refactor with a nicer UI (framework)
